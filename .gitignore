# Credentials — NEVER commit
.env
*.env
!.env.example

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/
*.egg-info/

# Build / cache
cache.db
*.db
*.db-journal
*.db-wal
*.db-shm

# Scanner outputs (data, not code)
cardtrader_scan_*.xlsx
cardtrader_core_*.xlsx
cardtrader_hype_*.xlsx
cardtrader_dead_*.xlsx
cardtrader_relatorio_*.xlsx
cardtrader_scanner.log
scan_*.log
*.log
# v2.6 checkpoint sidecars (crash-recovery JSONL — data, não código)
*.checkpoint.jsonl
# PR-F heartbeat (liveness runtime do scanner — data, não código)
heartbeat.txt

# MYP / merge tool outputs (sibling tools)
merge_*.xlsx
myp_scan_*.xlsx
validated_opps.json
graphify-out/
debug_*.html

# v2.4 operational state — local skip-list (sets que estouraram timeout).
# NOT committed: cada máquina pode ter cobertura/conexão diferente.
scanner_skip_list.json
# v2.7 portalocker sidecar
scanner_skip_list.json.lock
# v2.14 run guard lock (liveness runtime — concurrency guard, não código)
scanner_run.lock
scanner_skip_list.json.tmp*
# skip-list backups manuais (data, não código)
scanner_skip_list*.bak*
scanner_skip_list.backup*

# Outputs e logs — dados, não código (saneamento 2026-06-05)
outputs/
logs/

# Scratch de sessão/ad-hoc (diag, audits one-shot) — não versionar.
# Os scripts canônicos em scripts/ usam prefixos launch_/run_/test_/peek_,
# nunca "_". O "_" é a convenção de scratch.
_*.py
_*.ps1
_*.csv
AUDIT_overnight_*.md

# Firecrawl scrapes (validation cache)
.firecrawl/

# Office lock files
.~lock.*

# OS / sync artifacts
.fuse_hidden*
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# ── Public-release hygiene (2026-06-18): keep strategy/deal artifacts OUT ──
# Internal handoffs, session summaries and run state are operational data, not
# code. They reveal which sets profit, margins and findings — keep them local.
SESSION-HANDOFF.md
HANDOFF*.md
*handoff*.md
docs/RESUMO-*.md
docs/handoff*.md
# Live-partials / checkpoint run state (was pushed to scan-live branch — retired)
partials/
scan-state/
scan-live/

# Ad-hoc run output (deal/validation data — local only)
runs/
validate_*.py
