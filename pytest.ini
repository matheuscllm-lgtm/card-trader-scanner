[pytest]
# The pytest suite lives in tests/. Files under scripts/ named test_*.py are
# standalone run-scripts (they call sys.exit() at import time), not pytest
# tests — scoping collection here keeps `python -m pytest` clean.
testpaths = tests
